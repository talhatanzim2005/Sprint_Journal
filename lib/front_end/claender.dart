import 'package:flutter/material.dart';

class Claender extends StatelessWidget {
  const Claender({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Claender Screen'),
      ),
    );
  }
}
